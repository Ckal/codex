# Public Sample Assets

These files are intentionally small and public-safe. They are used for local verification,
Playwright screenshots, and documentation examples.

- `plant_leaf.ppm` and `plant_leaf.png` are generated bitmap-style plant samples for upload workflow testing.
  It is not a botanical benchmark image and should not be used to claim model accuracy.
